name: CI

on:
  push:
    branches:
      - master
      
jobs:
  bar:
    runs-on: ubuntu-latest
    steps:
      - name: Display event information
        run: |
          echo "event is: ${{ toJson(github) }}"
